# Keep repository text canonical across Windows and Linux CI.
* text=auto eol=lf

# Binary artifacts must never receive text conversion.
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.zip binary
*.tar.gz binary
